django.contrib.admin.sites
==========================

.. py:module:: django.contrib.admin.sites


Attributes
----------

.. autoapisummary::

   django.contrib.admin.sites.all_sites
   django.contrib.admin.sites.site


Exceptions
----------

.. autoapisummary::

   django.contrib.admin.sites.AlreadyRegistered
   django.contrib.admin.sites.NotRegistered


Classes
-------

.. autoapisummary::

   django.contrib.admin.sites.AdminSite
   django.contrib.admin.sites.DefaultAdminSite


Module Contents
---------------

.. py:data:: all_sites

.. py:exception:: AlreadyRegistered

   Bases: :py:obj:`Exception`


   Common base class for all non-exit exceptions.


.. py:exception:: NotRegistered

   Bases: :py:obj:`Exception`


   Common base class for all non-exit exceptions.


.. py:class:: AdminSite(name='admin')

   An AdminSite object encapsulates an instance of the Django admin application, ready
   to be hooked in to your URLconf. Models are registered with the AdminSite using the
   register() method, and the get_urls() method can then be used to access Django view
   functions that present a full admin interface for the collection of registered
   models.


   .. py:attribute:: site_title


   .. py:attribute:: site_header


   .. py:attribute:: index_title


   .. py:attribute:: site_url
      :value: '/'



   .. py:attribute:: enable_nav_sidebar
      :value: True



   .. py:attribute:: empty_value_display
      :value: '-'



   .. py:attribute:: login_form
      :value: None



   .. py:attribute:: index_template
      :value: None



   .. py:attribute:: app_index_template
      :value: None



   .. py:attribute:: login_template
      :value: None



   .. py:attribute:: logout_template
      :value: None



   .. py:attribute:: password_change_template
      :value: None



   .. py:attribute:: password_change_done_template
      :value: None



   .. py:attribute:: final_catch_all_view
      :value: True



   .. py:attribute:: name
      :value: 'admin'



   .. py:method:: check(app_configs)

      Run the system checks on all ModelAdmins, except if they aren't
      customized at all.



   .. py:method:: register(model_or_iterable, admin_class=None, **options)

      Register the given model(s) with the given admin class.

      The model(s) should be Model classes, not instances.

      If an admin class isn't given, use ModelAdmin (the default admin
      options). If keyword arguments are given -- e.g., list_display --
      apply them as options to the admin class.

      If a model is already registered, raise AlreadyRegistered.

      If a model is abstract, raise ImproperlyConfigured.



   .. py:method:: unregister(model_or_iterable)

      Unregister the given model(s).

      If a model isn't already registered, raise NotRegistered.



   .. py:method:: is_registered(model)

      Check if a model class is registered with this `AdminSite`.



   .. py:method:: add_action(action, name=None)

      Register an action to be available globally.



   .. py:method:: disable_action(name)

      Disable a globally-registered action. Raise KeyError for invalid names.



   .. py:method:: get_action(name)

      Explicitly get a registered global action whether it's enabled or
      not. Raise KeyError for invalid names.



   .. py:property:: actions

      Get all the enabled actions as an iterable of (name, func).


   .. py:method:: has_permission(request)

      Return True if the given HttpRequest has permission to view
      *at least one* page in the admin site.



   .. py:method:: admin_view(view, cacheable=False)

      Decorator to create an admin view attached to this ``AdminSite``. This
      wraps the view and provides permission checking by calling
      ``self.has_permission``.

      You'll want to use this from within ``AdminSite.get_urls()``:

          class MyAdminSite(AdminSite):

              def get_urls(self):
                  from django.urls import path

                  urls = super().get_urls()
                  urls += [
                      path('my_view/', self.admin_view(some_view))
                  ]
                  return urls

      By default, admin_views are marked non-cacheable using the
      ``never_cache`` decorator. If the view can be safely cached, set
      cacheable=True.



   .. py:method:: get_urls()


   .. py:property:: urls


   .. py:method:: each_context(request)

      Return a dictionary of variables to put in the template context for
      *every* page in the admin site.

      For sites running on a subpath, use the SCRIPT_NAME value if site_url
      hasn't been customized.



   .. py:method:: password_change(request, extra_context=None)

      Handle the "change password" task -- both form display and validation.



   .. py:method:: password_change_done(request, extra_context=None)

      Display the "success" page after a password change.



   .. py:method:: i18n_javascript(request, extra_context=None)

      Display the i18n JavaScript that the Django admin requires.

      `extra_context` is unused but present for consistency with the other
      admin views.



   .. py:method:: logout(request, extra_context=None)

      Log out the user for the given HttpRequest.

      This should *not* assume the user is already logged in.



   .. py:method:: login(request, extra_context=None)

      Display the login form for the given HttpRequest.



   .. py:method:: autocomplete_view(request)


   .. py:method:: catch_all_view(request, url)


   .. py:method:: get_app_list(request, app_label=None)

      Return a sorted list of all the installed apps that have been
      registered in this site.



   .. py:method:: index(request, extra_context=None)

      Display the main admin index page, which lists all of the installed
      apps that have been registered in this site.



   .. py:method:: app_index(request, app_label, extra_context=None)


.. py:class:: DefaultAdminSite

   Bases: :py:obj:`django.utils.functional.LazyObject`


   A wrapper for another class that can be used to delay instantiation of the
   wrapped class.

   By subclassing, you have the opportunity to intercept and alter the
   instantiation. If you don't need to do that, use SimpleLazyObject.


.. py:data:: site

