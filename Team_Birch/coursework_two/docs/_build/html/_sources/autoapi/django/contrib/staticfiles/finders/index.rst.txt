django.contrib.staticfiles.finders
==================================

.. py:module:: django.contrib.staticfiles.finders


Attributes
----------

.. autoapisummary::

   django.contrib.staticfiles.finders.searched_locations


Classes
-------

.. autoapisummary::

   django.contrib.staticfiles.finders.BaseFinder
   django.contrib.staticfiles.finders.FileSystemFinder
   django.contrib.staticfiles.finders.AppDirectoriesFinder
   django.contrib.staticfiles.finders.BaseStorageFinder
   django.contrib.staticfiles.finders.DefaultStorageFinder


Functions
---------

.. autoapisummary::

   django.contrib.staticfiles.finders.find
   django.contrib.staticfiles.finders.get_finders
   django.contrib.staticfiles.finders.get_finder


Module Contents
---------------

.. py:data:: searched_locations
   :value: []


.. py:class:: BaseFinder

   A base file finder to be used for custom staticfiles finder classes.


   .. py:method:: check(**kwargs)
      :abstractmethod:



   .. py:method:: find(path, all=False)
      :abstractmethod:


      Given a relative file path, find an absolute file path.

      If the ``all`` parameter is False (default) return only the first found
      file path; if True, return a list of all found files paths.



   .. py:method:: list(ignore_patterns)
      :abstractmethod:


      Given an optional list of paths to ignore, return a two item iterable
      consisting of the relative path and storage instance.



.. py:class:: FileSystemFinder(app_names=None, *args, **kwargs)

   Bases: :py:obj:`BaseFinder`


   A static files finder that uses the ``STATICFILES_DIRS`` setting
   to locate files.


   .. py:attribute:: locations
      :value: []



   .. py:attribute:: storages


   .. py:method:: check(**kwargs)


   .. py:method:: find(path, all=False)

      Look for files in the extra locations as defined in STATICFILES_DIRS.



   .. py:method:: find_location(root, path, prefix=None)

      Find a requested static file in a location and return the found
      absolute path (or ``None`` if no match).



   .. py:method:: list(ignore_patterns)

      List all files in all locations.



.. py:class:: AppDirectoriesFinder(app_names=None, *args, **kwargs)

   Bases: :py:obj:`BaseFinder`


   A static files finder that looks in the directory of each app as
   specified in the source_dir attribute.


   .. py:attribute:: storage_class


   .. py:attribute:: source_dir
      :value: 'static'



   .. py:attribute:: apps
      :value: []



   .. py:attribute:: storages


   .. py:method:: list(ignore_patterns)

      List all files in all app storages.



   .. py:method:: find(path, all=False)

      Look for files in the app directories.



   .. py:method:: find_in_app(app, path)

      Find a requested static file in an app's static locations.



.. py:class:: BaseStorageFinder(storage=None, *args, **kwargs)

   Bases: :py:obj:`BaseFinder`


   A base static files finder to be used to extended
   with an own storage class.


   .. py:attribute:: storage
      :value: None



   .. py:method:: find(path, all=False)

      Look for files in the default file storage, if it's local.



   .. py:method:: list(ignore_patterns)

      List all files of the storage.



.. py:class:: DefaultStorageFinder(*args, **kwargs)

   Bases: :py:obj:`BaseStorageFinder`


   A static files finder that uses the default storage backend.


   .. py:attribute:: storage


.. py:function:: find(path, all=False)

   Find a static file with the given path using all enabled finders.

   If ``all`` is ``False`` (default), return the first matching
   absolute path (or ``None`` if no match). Otherwise return a list.


.. py:function:: get_finders()

.. py:function:: get_finder(import_path)

   Import the staticfiles finder class described by import_path, where
   import_path is the full Python path to the class.


